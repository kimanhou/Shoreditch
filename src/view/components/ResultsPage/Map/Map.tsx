import React from 'react';
import './Map.scss';

interface IMapProps {
}

const Map : React.FC<IMapProps> = props => {

    return(
        <div className={`place-details-map`}>
        </div>
    )
}

export default Map;